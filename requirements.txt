# -*- coding: utf-8 -*-
apprise>=1.6.0
openai>=1.0.0
efinance>=0.4.0
akshare>=1.10.0
apscheduler>=3.10.0
httpx>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0
certifi
pytest>=7.0.0
tenacity>=8.2.0
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
playwright==1.57.0
PyJWT>=2.8.0
# 詳細資訊報告匯出 PDF(後臺直出,不依賴 Chromium):
# 主引擎 WeasyPrint(HTML 保真排版,需系統庫 pango/cairo + 中文字型,如 Docker 加 fonts-noto-cjk);
# WeasyPrint 不可用時回退 xhtml2pdf(純庫保底,中文用 reportlab STSong-Light)。
weasyprint>=60
xhtml2pdf>=0.2.17

# 深度分析 — TradingAgents (多 Agent 投資決策框架, 76k star)。
# 不在 PyPI,用 git+ URL 安裝。會拉 langchain/langgraph/yfinance 等較重依賴
# (~115 個包,首次安裝 2-5 分鐘)。
# 使用者啟用「TradingAgents 深度分析」Agent 時才會真正呼叫它;不啟用零開銷。
# 鎖定到 v0.5.0(release tag,非 @main)。該版本新增原生 portfolio 引數、按 filing
# 時間的 SEC EDGAR 基本面與獨立決策回測。適配層依賴的
# route_to_vendor/load_ohlcv/get_graph_args/create_initial_state 仍保持簽名穩定；
# REVIEW 不是可交易的 Hold，必須由 result_mapper 顯式展示為“待人工複核”。
tradingagents @ git+https://github.com/TauricResearch/TradingAgents.git@v0.5.0

# 本倉內獨立行情資料包(editable 本地安裝;發 PyPI 前不佔外部依賴)
-e ./packages/marketdata

# 業務無關的受限智慧體執行時(可單獨釋出)
-e ./packages/pan-agent-runtime

# Optional provider usage normalization and token measurement plugin.
-e ./packages/pan-agent-token-meter

# 可選的工具檢索外掛；PanWatch 預設啟用 shadow 模式。
-e ./packages/pan-agent-tool-research

# OpenTelemetry 匯出為「可選依賴組」,不在主依賴內(預設關閉,未裝即 no-op)。
# 需要把 trace 匯出到 Jaeger/Tempo/Langfuse 時:pip install -r requirements-otel.txt
# 詳見 requirements-otel.txt 與 README「OTel 匯出」。
